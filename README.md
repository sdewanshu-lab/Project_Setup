# Project_Setup
A JavaScript file that automates the generation of a project folder with an src folder containing index.html, style.css and main.js files.
Based on the content in these three generated file, we can see an expected output after running index.html with live server.
